import * as ListenerImport from './listener.module';
import * as SpeakerImport from './speaker.module';

export namespace Audio {
  export const ListenerModule = ListenerImport.ListenerModule;
  export const SpeakerModule = SpeakerImport.SpeakerModule;
}